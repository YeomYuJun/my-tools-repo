# handoff skill build — session handoff

> This document was generated by **applying the handoff skill to itself** — a real-use
> sample and the input for the §6 cold-session acceptance test. Tag on each item:
> `✓` source-verified · `~` inferred · `⚠️` unverified/conflicting.

## Goal
Complete the session-handoff skill (MVP) under `Skills/handoff/`. Keep the code thin;
**judge value (usefulness/efficiency) via a real heavy-session test** before deciding on
expansion. `~`

## Reference docs
- **Spec**: `DESIGN.md` (not included in this repo) — rationale, 4-step protocol, document schema, acceptance criteria.
- **Ledger + plan**: `PLAN.md` (not included in this repo) — the task checkboxes are the progress ledger. `✓`
- This file is the output example.

## Rules that must hold
1. **Commit gating**: the tree is not clean but **staged**. `git commit` **only on explicit user approval**. 0 commits so far. `✓` (git log)
2. **Module**: CommonJS (`require`/`module.exports`), consistent with the `state.js` precedent. `~`
3. **Placement**: `Skills/handoff/` (repo README 1:1 rule). Design/plan docs are kept here too (deliberate override). `~`
4. **No secrets**: in any artifact. `~`

## Established per-category policy (conversation-based judgment — the core value of this handoff)
- **Recall is done by the discovery bundle; the verification grades only raise precision.** Do not write "✓/~/⚠️ solves compaction-loss" — forgotten items get no grade at all. `~`
- **Sources are discovered per session.** Hardcoding a git+checklist+.state harvester is a dead design — even this session's ledger (PLAN.md) fell outside `handoff.js`'s pattern and went undetected, covered only by conversation memory. `~`
- **The `~` grade is not second-class — it is the center of value.** The highest value (accumulated judgment) usually lives only in conversation, so it becomes `~`. Give SYNTHESIZE equal rigor to VERIFY. `~`
- **Design split**: `handoff.js` is separated into a pure core (parse/classify/format, unit-tested) and an IO shell (git/fs) → tests cover the pure core, IO is smoke-checked. `✓` (code)
- **git diff is summarized/capped** (per-file +/- line counts, top N). Never embed a full diff = protect the next session's tokens. `✓`
- **Value skepticism honored**: the user is not yet convinced of usefulness/efficiency/value → no gold-plating, prove fast with an MVP. `~`

## Workflow
Execute PLAN.md task-by-task via `executing-plans`. Each code task is TDD (failing test → implement → pass),
staging (`git add`) only at each task end (commit deferred). User review at each checkpoint. `~`

## Current state
Done:
- ✅ **T1** pure core `summarizeDiff`/`classifyCandidates` + 5 tests. `✓`
- ✅ **T2** `formatBundle` (MD/JSON) + 2 tests → 7 total pass. `✓` (node --test)
- ✅ **T3** IO shell + CLI. MD/JSON smoke pass, regression 7 pass. `✓`
- ✅ **T4** `SKILL.md` 4-step protocol + structural check. `✓`
- ✅ **T5** dogfood — this document. `✓`
- ✅ **T6** `Skills/handoff/README.md` + root README "도구 목록" entry. `✓` (diff)
- ✅ **Post-build hardening** (review feedback): ledger-detection widened + "bucket is only a hint", VERIFY `✓`-must-cite rule → **8th test added, 8 pass**. `✓` (node --test)
- ✅ **i18n**: SKILL.md (description + body) and `handoff.js` bundle output converted to English (Claude-facing). `✓` (diff)

Remaining (all user-gated):
- ⚠️ **Value (usefulness/efficiency) unverified** — awaiting a real heavy-session test vs the user's manual baseline. If this handoff lets a new session resume without re-asking, that's first evidence of value. `⚠️`
- ⬜ **Commit** — only on explicit user approval. 0 commits, all staged. `✓` (git log)

Staged (uncommitted) files: `handoff.js`, `handoff.test.js`, `SKILL.md`, `README.md`, `EXAMPLE-handoff.md`, root `README.md` (M). DESIGN.md/PLAN.md are untracked. `✓`

## How the next session starts
1. Run `node Skills/handoff/handoff.js` for the current bundle → git state / staging.
2. Run `node --test Skills/handoff/handoff.test.js` (expect 8 pass) for code health.
3. Only **two judgment calls remain**: (a) finish the value verdict for this handoff, (b) whether to commit — ask the user on both. No code work.
4. To expand, start a new spec from `DESIGN.md` §2 out-of-scope (resume mode, state hook integration).

---
## Value verdict (vs manual — to be filled in)
- Accuracy (source-verified): "done / 8 pass" for T1–T6 is backed by git·tests as `✓` — a manual summary would rely on memory.
- Token savings: the bootstrap (~5 lines) does not duplicate this document (~60 lines).
- Honesty: value-unverified is surfaced explicitly as `⚠️` — a manual summary could gloss it as "almost done".
- **Final verdict: confirmed by the real heavy-session test result (user).**
